{
  "name": "@mpm/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mpm/contracts": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
